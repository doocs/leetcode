class Solution {
    public int addDigits(int num) {
        if(num <= 0) return num;
        do{
            int temp = 0;
            while(num > 0){
                temp += num % 10;
                num /= 10;
            }
            num = temp;
        }while(num > 9);
        return num;
    }
}
